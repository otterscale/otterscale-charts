{{- if and .Values.istio.enabled .Values.istio.virtualService.web.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "otterscale.fullname" . }}-web
  namespace: istio-system
  labels:
    {{- include "otterscale.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  hosts:
    - {{ include "otterscale.fullname" . }}-web.{{ .Release.Namespace }}.svc.cluster.local
    {{- range .Values.istio.virtualService.web.hosts }}
    - {{ . }}
    {{- end }}
  gateways:
    - {{ $.Values.istio.gateway.name }}
    - mesh
  http:
    - match:
      - uri:
          prefix: /
      route:
        - destination:
            host: {{ include "otterscale.fullname" $ }}-web.{{ .Release.Namespace }}.svc.cluster.local
            port:
              number: {{ $.Values.otterscaleWeb.service.port }}
      timeout: 30s
      retries:
        attempts: 3
        perTryTimeout: 10s
{{- end }}
